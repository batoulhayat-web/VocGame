<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Miss Batool — Vocabulary Games</title>
<style>
  :root{
    --bg1:#fffafc; --bg2:#f0faff; --mint:#c9f5df; --sky:#bfe4ff;
    --peach:#ffe0c2; --lilac:#e7d9ff; --rose:#ffd6e7; --ink:#334155;
  }
  *{box-sizing:border-box}
  html,body{height:100%;margin:0;font-family:'Poppins',sans-serif;background:linear-gradient(145deg,var(--bg1),var(--bg2));color:var(--ink);display:grid;place-items:center}
  .wrap{width:min(960px,92%);padding:20px}
  .card{background:#fff;border-radius:26px;padding:28px;box-shadow:0 12px 40px rgba(0,0,0,.08);position:relative;text-align:center}
  header h1{margin:0;font-size:clamp(22px,3.6vw,34px)}
  .sub{opacity:.7;margin:8px 0 28px}
  .avatar{
    width:220px;height:220px;margin:0 auto 20px;
    background:linear-gradient(135deg,var(--sky),var(--rose));
    border-radius:50%;display:grid;place-items:center;overflow:hidden;
    box-shadow:0 8px 24px rgba(0,0,0,.08);
  }
  .avatar img{width:100%;height:100%;object-fit:cover}
  .buttons{display:flex;flex-wrap:wrap;gap:16px;justify-content:center;margin-top:10px}
  .btn{
    min-width:220px;border:none;border-radius:16px;padding:16px 22px;cursor:pointer;
    font-weight:900;font-size:18px;color:#1f2a44;text-decoration:none;display:inline-flex;align-items:center;justify-content:center;
    background:linear-gradient(135deg,var(--rose),var(--sky));
    box-shadow:0 10px 24px rgba(0,0,0,.09);
    position:relative;overflow:hidden;
  }
  .btn.part2{background:linear-gradient(135deg,var(--mint),var(--lilac));}
  .btn::after{
    content:"✨";position:absolute;opacity:0;transform:scale(0.8);transition:all 0.6s ease;
    font-size:22px;top:50%;left:50%;transform:translate(-50%,-50%) scale(0.8);
  }
  .btn:hover::after{opacity:1;transform:translate(-50%,-50%) scale(1.3);}
  .btn:hover{transform:translateY(-2px);box-shadow:0 14px 30px rgba(0,0,0,.12);}
  .wm{position:absolute;right:12px;bottom:10px;font-weight:800;opacity:.6;background:#ffffffc8;padding:6px 10px;border-radius:999px;border:1px solid #e5e7eb}
</style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <header><h1>Vocabulary Games — Unit 3</h1></header>
      <div class="sub">Choose a game to play and earn your certificate 🎓</div>
      <div class="avatar">
        <img src="assets/character-cat.png" alt="Miss Batool character with cat-ear gaming headphones" onerror="this.style.display='none';">
      </div>
      <div class="buttons">
        <a class="btn" href="./part1/">Play Part 1</a>
        <a class="btn part2" href="./part2/">Play Part 2</a>
      </div>
      <div class="wm">Miss Batool</div>
    </div>
  </div>
</body>
</html>
